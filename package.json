{
  "name": "@b9g/zealot-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Schema-driven database client for TypeScript. Zod schemas define storage, validation, and form fields.",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '@b9g/zealot' build && bun run --filter './packages/zealot-*' build",
    "test": "bun test",
    "clean": "rm -rf packages/*/dist"
  },
  "devDependencies": {
    "@types/bun": "^1.3.4",
    "typescript": "^5.7.3"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/bikeshaving/zealot.git"
  },
  "keywords": [
    "database",
    "sql",
    "zod",
    "schema",
    "typescript",
    "orm",
    "query-builder"
  ],
  "author": "Brian Kim",
  "license": "MIT"
}
